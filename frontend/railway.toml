[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npx serve dist -s --listen tcp://0.0.0.0:$PORT"
healthcheckPath = "/"
healthcheckTimeout = 100

[nixpacks]
providers = ["node"]
